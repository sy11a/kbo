---
case: BL-417
issue: https://github.com/sy11a/Architector/issues/448
kind: Changed
date: 2026-09-23
---

## changelog

- **Edition 27, cohort C.** The AI layer moves **23 → 27**, four editions in one act: eight owned rule files take the delivered edition's text, one retired rule is deleted, and two Step 4 artifacts this repository never had are created — the fragment home `docs/changes/` and `.claude/rules/verification.md`, whose rows are written from this repository's own declaration rather than invented. `docs/adr/0001-record-architecture-decisions.md` is Step 4's one target deliberately **not** created here: this repository numbers its ADRs from `0000`, where that decision already lives, and `0001` is `event-envelope` — scaffolding the template would have given it two different ADRs under one number. No declaration here named the retired `docs/ai/engine.py` ([Architector#448](https://github.com/sy11a/Architector/issues/448)).

## journal

- Cohort C was the one the ordering worried about — four editions of accumulated change against cohorts A and B's one and two — and it upgraded no worse than the 25s: same eight owned files, same single deletion, no `exit 4`, nothing refused. The dry run over scratch copies said so before the act, and the act agreed. Two things the upgrade does not do and should not: `docs/journal/<today>.md` is not written, because an upgrade writes no entry — the day's work belongs to the upgrading repository; and Step 4's ADR seed is skipped where the repository's own numbering already holds that decision under another number. Both are the same rule read twice: a scaffold creates what is absent, not what is merely differently named.
